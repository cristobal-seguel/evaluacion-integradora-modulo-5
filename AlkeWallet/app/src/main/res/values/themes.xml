<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AlkeWallet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AlkeWallet" parent="Base.Theme.AlkeWallet" />


    <style name="Theme.SplashScreen.AlkeWallet" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary_alke_wallet</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_svg</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.AlkeWallet</item>
    </style>

    <style name="Theme.SplashScreen.Drawable" parent="Base.Theme.AlkeWallet">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
</resources>